
export interface Metric {
    id: string;             // entspricht p.id
    parameter: string;      // entspricht p.parameter
    id_verfahren: number;   // entspricht p.id_verfahren
    id_komp: number;        // entspricht p.id_komp
  }
  